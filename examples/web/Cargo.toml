[package]
name = "oidn-rs-web"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
web-sys = { version = "0.3.82", features = [
    "DomRect",
    "Element",
    "HtmlElement",
    "KeyboardEvent",
    "PointerEvent",
] }
yew = { version = "0.21.0", features = ["csr"] }
